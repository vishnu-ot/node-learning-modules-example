const add = (num1, num2) => {
  return num1 + num2;
};

const div = (num1, num2) => {
  return num1 / num2;
};

const sub = (num1, num2) => {
  return num1 - num2;
};



module.exports = {add, div, sub}